---
aliases: 
tags: 
date: "2025-12-07"
time: "17:44"
---
# 正交性
# 可读性 
# 可靠性

**Declarative languages**: the focus is on **what** the computer is to do 声明式
**Imperative languages**: the focus is on **how** the computer should do it. 命令式
If a language structure has more than one parse tree,  then the meaning of the structure cannot be  determined **uniquely.**
如果一个语言结构有多个解析树，则该结构的含义无法唯一确定。
The brackets, braces, and parentheses in the EBNF  extensions are **metasymbols** ,which means they are  notational tools and not terminal symbols.
------------------------------------------------
The basic elements of a specification of a data type are as follows: 
The **attributes** that distinguish data objects of that type, 
The **values** that data objects of that type may have, and 
The **operations** that define the possible manipulations of data objects of that type
属性,值, 操作符

A **descriptor** is the collection of the attributes of a variable.描述符
A variable is an abstraction of a memory cell变量是记忆单元的抽象
A binding is an association between an entity and an attribute 绑定是实体和属性之间的关联
A binding is **static** if it first occurs before run time and remains unchanged throughout program execution.

A binding is **dynamic** if it first occurs during execution or can change during execution of the program

Allocation:

Deallocation:

**The lifetime**
**Static variables**
**Stack-dynamic variables**
**Explicit heap-dynamic variables**
**Implicit heap-dynamic variables**隐式对动态变量
The **scope** of a variable is the range of statements over which it is visible.作用域

The collection of values that can be represented by a floating point type is defined in terms of **precision** and **range**
浮点可以定义精度和范围

**Decimal** data types store a fixed number of decimal digits, with the decimal point at a fixed position in the value.
十进制
Complex 复数
A **character string type** is one in which the values consist of sequences of characters.
字符串
**Static length string:**
**limited dynamic length string:**
**Dynamic length strings**
List operations in Scheme
CAR returns the first element of its list parameter (CAR ’(A B C)) returns A
CDR returns the remainder of its list parameter after the first element has been removed (CDR ’(A B C)) returns (B C)
CONS puts its first parameter into its second parameter, a list, to make a new list (CONS ’A ’(B C)) returns (A B C)
LIST returns a new list of its parameters (LIST ’A ’B ’(C D)) returns (A B (C D)
八大控制机制：

1. 顺序（Sequencing）
    
2. 选择（Selection）
    
3. 循环（Iteration）
    
4. 过程抽象（Procedural Abstraction）:function
    
5. 递归（Recursion）
    
6. 并发（Concurrency）
    
7. 异常（Exception）
    
8. 非确定性（Nondeterminacy）

All algorithms represented by flowcharts can be coded with only **two-way selection** and **pretest logical loops**